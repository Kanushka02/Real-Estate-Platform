# 🏗️ Real Estate Platform - Architecture Documentation

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌───────────┐ │
│  │   Browser  │  │   Mobile   │  │   Tablet   │  │  Desktop  │ │
│  └────────────┘  └────────────┘  └────────────┘  └───────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/HTTPS
                             │ Port 3000
┌────────────────────────────┴────────────────────────────────────┐
│                    PRESENTATION LAYER (React)                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Components: Navbar, Footer, PropertyCard, Filter, etc. │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Pages: Home, Login, Properties, Admin, etc.            │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Context: AuthContext (State Management)                │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Services: API Service (Axios)                          │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Routing: React Router v6                               │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Styling: Tailwind CSS                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │ REST API (JSON)
                             │ Port 8080
┌────────────────────────────┴────────────────────────────────────┐
│                   APPLICATION LAYER (Spring Boot)                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Controllers: Auth, Property, Favorite, Admin           │   │
│  │  ├── Request Mapping                                    │   │
│  │  ├── Validation                                         │   │
│  │  └── Response Formatting                                │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Security Layer                                         │   │
│  │  ├── JWT Authentication Filter                          │   │
│  │  ├── Spring Security Configuration                      │   │
│  │  └── Role-based Access Control                          │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Service Layer (Business Logic)                         │   │
│  │  ├── PropertyService                                    │   │
│  │  ├── FavoriteService                                    │   │
│  │  └── UserService                                        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  Repository Layer (Data Access)                         │   │
│  │  ├── UserRepository (JPA)                               │   │
│  │  ├── PropertyRepository (JPA)                           │   │
│  │  ├── RoleRepository (JPA)                               │   │
│  │  └── FavoriteRepository (JPA)                           │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │ JDBC
                             │ Port 5432
┌────────────────────────────┴────────────────────────────────────┐
│                    DATA LAYER (PostgreSQL)                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Tables:                                                │   │
│  │  ├── users                                              │   │
│  │  ├── roles                                              │   │
│  │  ├── user_roles                                         │   │
│  │  ├── properties                                         │   │
│  │  └── favorites                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Component Architecture

### Frontend Component Hierarchy

```
App (AuthProvider)
├── Router
    ├── Navbar
    │   ├── Logo
    │   ├── Navigation Links
    │   ├── User Menu
    │   └── Mobile Menu
    ├── Routes
    │   ├── Home
    │   │   ├── Hero Section
    │   │   ├── Search Bar
    │   │   ├── Featured Properties (PropertyCard[])
    │   │   └── Latest Properties (PropertyCard[])
    │   ├── Properties
    │   │   ├── PropertyFilter
    │   │   ├── PropertyCard[]
    │   │   └── Pagination
    │   ├── PropertyDetails
    │   │   ├── Image Gallery
    │   │   ├── Property Info
    │   │   ├── Features List
    │   │   └── Contact Sidebar
    │   ├── PropertyForm (Create/Edit)
    │   │   ├── Basic Info Section
    │   │   ├── Location Section
    │   │   ├── Details Section
    │   │   └── Additional Info Section
    │   ├── MyProperties
    │   │   └── PropertyCard[] (with Edit/Delete)
    │   ├── Favorites
    │   │   └── PropertyCard[]
    │   ├── AdminDashboard
    │   │   ├── Properties Tab
    │   │   │   └── Properties Table
    │   │   └── Users Tab
    │   │       └── Users Table
    │   ├── Login
    │   └── Signup
    └── Footer
        ├── About
        ├── Quick Links
        └── Contact Info
```

### Backend Layer Architecture

```
┌─────────────────────────────────────────┐
│          Controller Layer               │
│  - Handles HTTP requests                │
│  - Validates input                      │
│  - Returns responses                    │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│          Security Layer                 │
│  - JWT Token Validation                 │
│  - User Authentication                  │
│  - Authorization Checks                 │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│          Service Layer                  │
│  - Business Logic                       │
│  - Data Transformation (DTO)            │
│  - Transaction Management               │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│          Repository Layer               │
│  - Database Queries                     │
│  - JPA Operations                       │
│  - Custom Queries                       │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│          Database Layer                 │
│  - PostgreSQL                           │
│  - Tables & Relations                   │
│  - Constraints & Indexes                │
└─────────────────────────────────────────┘
```

## Data Flow Diagrams

### User Authentication Flow

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │────▶│  Login   │────▶│   Auth   │────▶│   User   │
│          │     │   Page   │     │Controller│     │Repository│
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     ▲                                   │                │
     │                                   ▼                ▼
     │                            ┌──────────┐     ┌──────────┐
     │                            │   JWT    │     │ Database │
     └────────────────────────────│  Utils   │     │          │
              JWT Token           └──────────┘     └──────────┘
```

### Property Listing Flow

```
User Actions
    │
    ├─── Browse ────────┐
    │                   ▼
    ├─── Search ───▶ Properties Page
    │                   │
    ├─── Filter ────────┘
    │                   │
    │                   ▼
    │            PropertyController
    │                   │
    │                   ▼
    │            PropertyService
    │                   │
    │                   ▼
    │           PropertyRepository
    │                   │
    │                   ▼
    │               Database
    │                   │
    │                   ▼
    └─── View ◀───── Results
```

### Property Creation Flow (with Admin Approval)

```
User
 │
 ├─ Post Property
 │       │
 │       ▼
 │  PropertyForm
 │       │
 │       ▼
 │  PropertyController
 │       │
 │       ▼
 │  PropertyService
 │       │
 │       ▼
 │  Save with Status: PENDING
 │       │
 │       ▼
 │   Database
 │
 ├─ Admin Views in Dashboard
 │       │
 │       ▼
 │  AdminController
 │       │
 │       ├─ Approve ──▶ Status: APPROVED ──▶ Visible to Public
 │       │
 │       └─ Reject ───▶ Status: REJECTED ──▶ Hidden from Public
```

## Database Schema

### Entity Relationship Diagram

```
┌─────────────────┐         ┌─────────────────┐
│     USERS       │◀───────▶│     ROLES       │
├─────────────────┤         ├─────────────────┤
│ id (PK)         │         │ id (PK)         │
│ username        │         │ name            │
│ email           │         └─────────────────┘
│ password        │               │
│ full_name       │               │
│ phone           │               │
│ active          │         (user_roles)
│ created_at      │         Many-to-Many
│ updated_at      │
└────────┬────────┘
         │
         │ One-to-Many
         │
         ▼
┌─────────────────┐
│   PROPERTIES    │
├─────────────────┤
│ id (PK)         │
│ title           │
│ description     │
│ type            │
│ listing_type    │
│ price           │
│ address         │
│ city            │
│ district        │
│ bedrooms        │
│ bathrooms       │
│ land_size       │
│ floor_size      │
│ status          │
│ owner_id (FK)   │
│ created_at      │
│ updated_at      │
└────────┬────────┘
         │
         │ One-to-Many
         │
         ▼
┌─────────────────┐
│   FAVORITES     │
├─────────────────┤
│ id (PK)         │
│ user_id (FK)    │
│ property_id(FK) │
│ created_at      │
└─────────────────┘
```

## Security Architecture

### JWT Authentication Flow

```
1. User Login
   ├─ Credentials sent to /api/auth/login
   │
2. Server validates credentials
   ├─ Check username/password
   ├─ BCrypt verification
   │
3. Generate JWT Token
   ├─ User info + roles
   ├─ Sign with secret key
   ├─ Set expiration (24h)
   │
4. Return JWT to client
   ├─ Client stores in localStorage
   │
5. Subsequent requests
   ├─ Client sends: Authorization: Bearer <token>
   ├─ Server validates token
   ├─ Extract user info
   └─ Grant/Deny access
```

### Authorization Matrix

| Role  | Public | Browse | Post | Edit Own | Delete Own | Favorites | Admin Panel |
|-------|--------|--------|------|----------|------------|-----------|-------------|
| Guest | ✅     | ✅     | ❌   | ❌       | ❌         | ❌        | ❌          |
| USER  | ✅     | ✅     | ✅   | ✅       | ✅         | ✅        | ❌          |
| ADMIN | ✅     | ✅     | ✅   | ✅       | ✅         | ✅        | ✅          |

## API Architecture

### REST API Design

```
/api
├── /auth
│   ├── POST /signup          (Public)
│   └── POST /login           (Public)
│
├── /properties
│   ├── GET /                 (Public - Approved only)
│   ├── GET /{id}            (Public)
│   ├── GET /search          (Public)
│   ├── GET /filter          (Public)
│   ├── GET /latest          (Public)
│   ├── GET /featured        (Public)
│   ├── GET /my-properties   (Authenticated)
│   ├── POST /               (Authenticated)
│   ├── PUT /{id}            (Authenticated - Owner)
│   └── DELETE /{id}         (Authenticated - Owner)
│
├── /favorites
│   ├── GET /                (Authenticated)
│   ├── POST /{propertyId}   (Authenticated)
│   ├── DELETE /{propertyId} (Authenticated)
│   └── GET /{propertyId}/check (Authenticated)
│
└── /admin
    ├── GET /properties      (Admin)
    ├── PUT /properties/{id}/approve (Admin)
    ├── PUT /properties/{id}/reject  (Admin)
    ├── DELETE /properties/{id}      (Admin)
    ├── GET /users           (Admin)
    └── PUT /users/{id}/toggle-status (Admin)
```

## Technology Stack Details

### Backend Technology Stack

```
Spring Boot 3.2.0
├── Spring Web (REST APIs)
├── Spring Security (Authentication)
├── Spring Data JPA (ORM)
├── PostgreSQL Driver
├── JWT (io.jsonwebtoken)
├── Lombok (Code generation)
└── Validation (Bean validation)
```

### Frontend Technology Stack

```
React 18
├── React Router DOM 6 (Routing)
├── Axios (HTTP client)
├── Tailwind CSS (Styling)
├── Context API (State management)
└── Create React App (Build tool)
```

## Deployment Architecture

### Development Environment

```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   Frontend      │      │    Backend      │      │   PostgreSQL    │
│   localhost     │◀────▶│   localhost     │◀────▶│   localhost     │
│   :3000         │ API  │   :8080         │ JDBC │   :5432         │
│   (React Dev)   │      │ (Spring Boot)   │      │  (Docker/Local) │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

### Production Environment (Example)

```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   CDN/Static    │      │   Application   │      │   Database      │
│   Hosting       │◀────▶│   Server        │◀────▶│   Server        │
│   (Netlify/     │ API  │   (AWS/Heroku/  │ JDBC │   (AWS RDS/     │
│    Vercel)      │      │    Digital      │      │    Heroku       │
│                 │      │    Ocean)       │      │    Postgres)    │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

## Performance Considerations

### Backend Optimizations
- JPA Lazy Loading for relationships
- Pagination for large datasets
- Database indexing on frequently queried fields
- Connection pooling
- Caching ready (can add Redis)

### Frontend Optimizations
- Code splitting ready
- Lazy loading routes
- Optimized images
- Tailwind CSS purge in production
- Bundle optimization

## Scalability

### Horizontal Scaling Ready
- Stateless backend (JWT)
- Database connection pooling
- Can add load balancer
- Can add multiple backend instances

### Vertical Scaling
- Optimized queries
- Efficient data structures
- Minimal memory footprint

---

**Architecture designed for scalability, security, and maintainability** 🏗️

