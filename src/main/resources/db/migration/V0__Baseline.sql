-- Baseline migration - existing tables created by Hibernate
-- This file serves as a baseline for existing database schema

-- Users table (already exists)
-- Properties table (already exists)
-- Locations table (already exists)
-- Property categories table (already exists)
-- Bookings table (already exists)

-- Note: This is a baseline migration for existing schema
-- New migrations will be applied on top of this